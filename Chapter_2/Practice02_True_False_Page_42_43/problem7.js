// tor ki raate khawa sesh ? seta niye isDinnerDone name ekti boolean variable declare kor | khawa sesh hole ki likhbi ar sesh na hole ki likhbi, sheta toh janos already |

// ANS

const isDinnerDone = true;

console.log(isDinnerDone);