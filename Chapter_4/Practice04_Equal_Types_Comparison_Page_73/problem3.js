// true
// here the result is true because we're checking with == which doesn't check the types
console.log(25 == "25");
// false 
// here the result is false because we're checking with === which also check the types
console.log(25 === "25");
