const isActive = true;
const isSubscribed = true;
const isPremium = false;
if (isActive) {
  if (isSubscribed) {
    if (isPremium) {
      console.log("You can get premium feature");
    } else {
      console.log("watch free version");
    }
  }
}
